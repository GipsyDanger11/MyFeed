# MyFeed automation worker dependencies
# Pinned loosely to avoid the instagrapi/pydantic/fastapi version war.
# NOTE: we talk to postgrest-py directly (not supabase-py) because the
# latest supabase-py has a double-/rest/v1/ URL bug.
fastapi>=0.115.0,<0.120
uvicorn[standard]>=0.32.0,<0.40
pydantic>=2.10.1,<3
postgrest>=2.17.0,<3
httpx>=0.27,<0.29
cryptography>=43.0.1,<46
instagrapi>=2.1.3
python-dotenv>=1.0.1
